<h1>Editing repository</h1>

<%= render 'form' %>

<%= link_to 'Show', repository_path(@repository.id, :organization_id => params[:organization_id]) %> |
<%= link_to 'Back', organization_path(params[:organization_id]) %>
